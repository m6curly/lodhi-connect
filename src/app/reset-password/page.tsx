'use client';import {useState} from 'react';import {useRouter} from 'next/navigation';import {createClient} from '@/lib/supabase/client';import {PasswordInput} from '@/components/PasswordInput';export default function Reset(){const [msg,setMsg]=useState('');const r=useRouter();async function go(e:any){e.preventDefault();const f=new FormData(e.currentTarget);if(f.get('p')!==f.get('c')){setMsg('Passwords do not match.');return}const {error}=await createClient().auth.updateUser({password:String(f.get('p'))});if(error)setMsg(error.message);else{setMsg('Password updated.');setTimeout(()=>r.push('/login'),800)}}return <main className="container py-20"><div className="max-w-md mx-auto card p-8"><div className="kicker">New password</div><h1 className="serif text-4xl mt-2">Set password</h1><form onSubmit={go} className="space-y-5 mt-7"><PasswordInput name="p"/><PasswordInput name="c" placeholder="Confirm password"/><button className="btn btn-primary w-full">Update password</button></form>{msg&&<p className="text-sm muted mt-4">{msg}</p>}</div></main>}

'use client';import {useState} from 'react';import {createClient} from '@/lib/supabase/client';export default function Forgot(){const [msg,setMsg]=useState('');async function go(e:any){e.preventDefault();const email=String(new FormData(e.currentTarget).get('email'));const {error}=await createClient().auth.resetPasswordForEmail(email,{redirectTo:`${location.origin}/reset-password`});setMsg(error?error.message:'If this email exists, a password reset link has been sent.')}return <main className="container py-20"><div className="max-w-md mx-auto card p-8"><div className="kicker">Account recovery</div><h1 className="serif text-4xl mt-2">Reset password</h1><form onSubmit={go} className="space-y-5 mt-7"><input className="input" name="email" type="email" required placeholder="you@gmail.com"/><button className="btn btn-primary w-full">Send reset link</button></form>{msg&&<p className="text-sm muted mt-4">{msg}</p>}</div></main>}
